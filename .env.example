# Optional — enables live AI refinement in /plan-trip
# Without a key, smart local refinement is used instead
# Get your Gemini API key from: https://aistudio.google.com/app/apikey

GEMINI_API_KEY=
OPENAI_API_KEY=
